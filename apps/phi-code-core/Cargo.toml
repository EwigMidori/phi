[package]
name = "phi-code-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "phi-code product runtime (session host over kernel SendQueue)"
readme = "README.md"

[dependencies]
phi-kernel = { path = "../../crates/phi-kernel" }
tokio.workspace = true

[lints]
workspace = true
