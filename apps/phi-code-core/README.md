# phi-code-core

Product runtime for phi-code: **turn orchestration** (`SessionTurnRunner`).

LLM provider adapters live in [`phi-ext-llm`](../../crates/phi-ext-llm).
TUI lives in [`phi-code-ui`](../phi-code-ui).

```bash
cargo test -p phi-code-core
```
